import { motion } from "framer-motion";
import { cn } from "@/lib/utils";
key = { index }
className = "list-none flex items-center text-sm before:content-['▹'] before:text-primary before:mr-2 before:text-xs"
  >
  { skill }
      </li >
    ))
  }
      </ul >
    </motion.div >
  );
};

export default SkillCard;